general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = sleep 2 && hyprctl dispatch dpms on
}

# 10 Minutes: Suspend the PC ONLY if the screen is locked
listener {
    timeout = 600
    on-timeout = pidof hyprlock && systemctl suspend
}
